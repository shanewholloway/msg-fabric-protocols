
const abstract_chan_interface = @{}
  send(pkt) ::

const abstract_sink_interface = @{}
  by_msgid: new Map()

  json_unpack(sz) :: return JSON.parse(sz)
  recvCtrl(msg, info) ::
  recvMsg(msg, info) :: return @{} msg, info
  recvStream(msg, info) ::
  recvStreamData(rstream, info) ::

